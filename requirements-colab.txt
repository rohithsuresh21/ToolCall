# Dependencies for training on Colab / cloud / laptop.
#
# NOTE: do NOT pin very old versions here. Colab runs Python 3.13, which has no
# wheels for torch<2.4 / peft<0.14. Use the modern stack Colab already ships
# (torch, transformers, peft, accelerate) and rely on the repo fixes in
# atr/train/sft.py (removed group_by_length, use dtype=, gradient_checkpointing
# defaulted OFF to avoid the peft/torchao detach bug on modern stacks).
#
# Install:   pip install -q torch transformers accelerate peft datasets
#            (or just run `pip install -q -r requirements-colab.txt`)

torch
transformers
accelerate
peft
datasets
